<script>
  const items = $signal([0, 1, 2]);
</script>

<div onclick={() => items.update(v => v.length === 4 ? [] : [...v, v.length])}>
  <p>
    start 
  </p>
  <p #for={item in items.get()}>
    {{ item }}
  </p>
  <p>
    end
  </p>
</div>
